{
  "name": "consultchimps-workspace",
  "version": "0.1.0",
  "private": true,
  "description": "Durable, composable operations tools for consultants.",
  "license": "Apache-2.0",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/consultchimps/consultchimps.git"
  },
  "type": "module",
  "packageManager": "pnpm@11.10.0",
  "engines": {
    "node": ">=22.13.0"
  },
  "scripts": {
    "build": "pnpm -r --if-present build",
    "bundle:cli": "pnpm --filter consultchimps bundle",
    "changeset": "changeset",
    "check": "pnpm format:check && pnpm lint && pnpm build && pnpm typecheck && pnpm test:coverage && pnpm package:check && pnpm docs:check",
    "consultchimps": "node packages/cli/dist/index.js",
    "docs:check": "node scripts/check-cli-reference.ts && node scripts/check-registry-site.ts && node scripts/check-claims.ts && node scripts/check-readme-operations.ts && node scripts/check-preservation-matrix.ts",
    "docs:dev": "pnpm --filter @consultchimps/docs dev",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "lint": "eslint --flag unstable_native_nodejs_ts_config . && pnpm -r --if-present lint",
    "package:check": "node scripts/check-package-dependencies.ts && node scripts/check-packages.ts",
    "release": "changeset publish",
    "test": "pnpm build && vitest run",
    "test:coverage": "vitest run --coverage",
    "test:run": "vitest run",
    "typecheck": "pnpm -r --if-present typecheck",
    "version-packages": "changeset version && pnpm install --lockfile-only"
  },
  "devDependencies": {
    "@arethetypeswrong/cli": "0.18.5",
    "@changesets/cli": "2.31.1",
    "@consultchimps/xlsx": "workspace:*",
    "@eslint/js": "10.0.1",
    "@types/node": "26.2.0",
    "@vitest/coverage-v8": "4.1.11",
    "eslint": "10.8.0",
    "prettier": "3.9.6",
    "publint": "0.3.24",
    "tsup": "8.5.1",
    "typescript": "6.0.3",
    "typescript-eslint": "8.67.0",
    "vitest": "4.1.11"
  }
}
