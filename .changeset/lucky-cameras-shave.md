---
"consultchimps": patch
---

Correct the package README: the CLI supports Node.js 22 or later, matching the
declared `engines.node`, rather than requiring Node.js 24. The command overview
now lists `pptx inspect-template` alongside the other commands and points at the
generated CLI reference for the full set of options.
