---
"@consultchimps/db": minor
---

Add `@consultchimps/db/sqlite-read` for temporary read-only SQLite catalogs in
Node and browser workers. The entry uses the official SQLite WASM runtime,
preserves 64-bit integers and binary values, provides configurable read limits,
and creates no persistent workspace files.
